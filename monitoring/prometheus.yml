global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
  
  # Example to monitor an application on a specific port
  # - job_name: 'cleanindia-backend'
  #   static_configs:
  #     - targets: ['host.docker.internal:3000']
